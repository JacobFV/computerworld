Reftest pairs that the engine does not pass yet; the M2 gate moves each back into ref/ when it does.

Empty: Acid2 passes pixel for pixel and lives in `tests/ref/` (compared at `#top`, see `tests/reftest.rs`).
